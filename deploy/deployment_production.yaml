apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: <namespace>
  name: blue-y
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::716156543026:role/BlueYBedrockRole
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: blue-y-readonly
rules:
  # Read-only access to pods, nodes, services, deployments, secrets (TLS only via code filter)
  - apiGroups: [""]
    resources: ["pods", "pods/log", "nodes", "services", "secrets", "events", "namespaces"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "replicasets"]
    verbs: ["get", "list", "watch"]
  # Allow restart and scale (patch deployments + scale subresource)
  - apiGroups: ["apps"]
    resources: ["deployments", "deployments/scale"]
    verbs: ["patch", "update"]
  # HPA monitoring
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "list", "watch"]
  # Pod metrics (CPU/memory usage via metrics-server)
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods", "nodes"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: blue-y-readonly-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: blue-y-readonly
subjects:
  - kind: ServiceAccount
    name: blue-y
    namespace: <namespace>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: <namespace>
  name: blue-y-<tag>
  labels:
    app: "blue-y"
    version: "<tag>"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "blue-y"
      version: "<tag>"
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: "blue-y"
        version: "<tag>"
    spec:
      serviceAccountName: blue-y
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      containers:
        - image: <ECR>:<tag>-<HASH>
          name: blue-y
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "8000"
            - name: AI_BASE_URL
              value: "https://api.deepseek.com/v1"
            - name: AI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: deepseek-api-key
            - name: AI_ROUTINE_MODEL
              value: "deepseek-chat"
            - name: AI_INCIDENT_MODEL
              value: "deepseek-reasoner"
            - name: WATCH_NAMESPACES
              value: "prod,doris,monitoring,wordpress"
            - name: KUBE_IN_CLUSTER
              value: "true"
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: telegram-bot-token
            - name: TELEGRAM_CHAT_ID
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: telegram-chat-id
            - name: TEAMS_APP_ID
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: teams-app-id
                  optional: true
            - name: TEAMS_APP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: teams-app-password
                  optional: true
            - name: TEAMS_TENANT_ID
              value: "7ef59ba4-e80a-48fd-841e-72fe93f5f3f4"
            - name: JIRA_BASE_URL
              value: "https://blueonion.atlassian.net"
            - name: JIRA_PROJECT_KEY
              value: "HUBS"
            - name: JIRA_EMAIL
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: jira-email
            - name: JIRA_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: blue-y-secrets
                  key: jira-api-token
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  namespace: <namespace>
  name: blue-y-<tag>
spec:
  type: ClusterIP
  selector:
    app: "blue-y"
    version: "<tag>"
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
